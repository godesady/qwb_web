# web
## 签到
第一关可以使用MD5的弱碰撞进行绕过，也可以使用数组进行绕过，第二关使用数组绕过，
（ps:使用数组可以同时绕过前两关），第三关使用MD5的强碰撞，使用fastcoll软件
## share you mind
这个题考察rpo洞的查找和利用，rop洞可以引入任意的js,利用自己构造好的js可以打到admin的cookies，
首先找到可以插入js的位置为自己发布的文章，不写标题。/index.php/view?位置存在相对路径引用，此处过滤了单引号，可以使用
String.fromCharCode函数进行绕过。接下来构造js打cookies，拿到hint：flag在/QWB_fl4g/QWB/下，则使用iframe获取此路径下的cookies，flag在其中。

遇到一种非预期解法，即使用:http://39.107.33.96:20000/?a=一个js向量可以打到cookies，拿到hint后同样使用iframe获取此路径下cookies。
## Three hit
这个题是一个二次注入，对age进行注入，注入时使用hex进行绕过，尝试找到回显位，找到后就可以直接回显出字段的值，接下来就是一步一步获得数据库名，表名，列名，字段值，找到flag
## 彩蛋
根据提示是命令注入，可是数据库可以直接使用默认参数进行连接，连接后使用类似MySQL的udf提权的方法注入c代码动态库，实现系统调用，最终找到flag，还有在docker中找到数据库连接参数的方法可以连接上数据库，接下来同样使用构造动态库的方法实现系统调用
## python1
此题提供源码，属于源码审计，注册处的email存在盲注，可以爆破出数据库相关信息，